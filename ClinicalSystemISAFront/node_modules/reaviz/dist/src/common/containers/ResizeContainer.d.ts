import { PureComponent, ReactNode } from 'react';
export interface ResizeEvent {
    height: number;
    width: number;
}
interface ResizeContainerProps {
    children: ReactNode;
    onSize: (event: ResizeEvent) => void;
    height?: number;
    width?: number;
}
export declare class ResizeContainer extends PureComponent<ResizeContainerProps> {
    SizeMe: any;
    constructor(props: ResizeContainerProps);
    shouldAutosize(): boolean;
    render(): JSX.Element;
}
export {};
